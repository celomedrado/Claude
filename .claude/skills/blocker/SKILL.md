---
name: blocker
description: Log a blocker to DM with impact and what is needed to unblock.
argument-hint: "<what is blocked + impact + what you need>"
disable-model-invocation: true
---

Use /call-agent DM with the blocker. DM must produce an unblock plan and handoffs.
